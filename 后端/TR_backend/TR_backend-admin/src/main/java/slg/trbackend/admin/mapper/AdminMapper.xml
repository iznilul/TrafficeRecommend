<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="slg.trbackend.admin.mapper.AdminMapper">

    <select id="loginCheck" resultType="slg.trbackend.admin.dto.Admin">
        select * from admin where username=#{username} and password=#{password}
    </select>

</mapper>